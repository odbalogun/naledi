# Payload CMS (Next app in /cms). When set, the site loads events, destinations,
# FAQs, and blog content from the API. Leave unset to use built-in sample copy only.
# VITE_CMS_URL=http://localhost:3001
